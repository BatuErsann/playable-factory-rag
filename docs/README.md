# Generated Documentation

Run `npm run docs` from the repository root to generate the Doxygen HTML reference in `docs/generated/html/`.

The generated files are intentionally excluded from version control.
